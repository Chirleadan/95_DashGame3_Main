# Optional — only needed for production (Render + PostgreSQL).
# Leave unset for local dev: SQLite is used automatically at backend/data/leaderboard.sqlite

# DATABASE_URL=postgresql://user:password@host:5432/dbname
# FRONTEND_ORIGIN=http://localhost:5173
# PORT=3001
