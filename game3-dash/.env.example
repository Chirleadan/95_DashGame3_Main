# Optional — copy to .env.local only if you want the global leaderboard in dev.
# Backend runs at http://localhost:3001 with zero config (SQLite, no DATABASE_URL).
VITE_API_BASE_URL=http://localhost:3001
